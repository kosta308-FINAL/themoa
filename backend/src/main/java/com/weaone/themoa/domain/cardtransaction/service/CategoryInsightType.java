package com.weaone.themoa.domain.cardtransaction.service;

/** 카테고리 소비 상세 인사이트 종류(categoryDetail.md §7). 응답 순서는 항상 이 선언 순서를 따른다. */
public enum CategoryInsightType {
    PERIOD_CHANGE,
    PEAK_PHASE,
    CYCLE_TREND
}
