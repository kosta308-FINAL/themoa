package com.weaone.themoa.domain.fixedexpense.entity;

/** {@link com.weaone.themoa.domain.cardtransaction.entity.PaymentMethod}과는 다른 열거형이다(거래는 CASH도 있다). */
public enum FixedExpensePaymentMethod {
    CARD,
    TRANSFER
}
